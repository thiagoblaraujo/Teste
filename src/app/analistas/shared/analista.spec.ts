/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Analista} from './analista';

describe('Analista', () => {
  it('should create an instance', () => {
    expect(new Analista()).toBeTruthy();
  });
});
